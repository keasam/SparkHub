import { useState } from 'react'; import { router } from 'expo-router'; import { Shell,Field } from '../src/ui';
export default function Professional(){const [role,setRole]=useState('');const [company,setCompany]=useState('');const [years,setYears]=useState('');const [skills,setSkills]=useState('');const [wins,setWins]=useState('');return <Shell step="STEP 04 · PROFESSIONAL" title="What have you done?" subtitle="Capture your experience, skills, work history, and meaningful outcomes." next={()=>router.push('/project')}><Field label="Current role / occupation" value={role} onChangeText={setRole} placeholder="Founder, student, product manager..."/><Field label="Current / most recent organization" value={company} onChangeText={setCompany} placeholder="Company or organization"/><Field label="Years of experience" value={years} onChangeText={setYears} placeholder="e.g. 5" keyboardType="number-pad"/><Field label="Core skills" value={skills} onChangeText={setSkills} placeholder="Product, sales, engineering, finance..." multiline/><Field label="Key achievements" value={wins} onChangeText={setWins} placeholder="Projects, launches, revenue, awards, leadership, outcomes..." multiline/></Shell>}
